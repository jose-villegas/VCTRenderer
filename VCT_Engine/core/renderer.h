#pragma once
namespace VCT_ENGINE
{
    // loads all required assets for the VCT_ENGINE renderer
    class Renderer
    {
        public:
            Renderer();
            virtual ~Renderer();
    };

}